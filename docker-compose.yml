services:
  postgres:
    image: postgres:16-alpine
    container_name: linuxcv-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: linuxcv
      POSTGRES_PASSWORD: linuxcv
      POSTGRES_DB: linuxcv
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U linuxcv']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
